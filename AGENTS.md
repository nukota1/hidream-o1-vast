## Task and Progress Management

このプロジェクトでは、以下のファイルを使用してタスクと進捗を管理する。

- `docs/PROJECT_STATUS.md`: プロジェクト全体の現在の状態
- `docs/TASKS.md`: タスク一覧と各タスクの進捗
- `docs/DECISIONS.md`: 重要な設計判断とその理由

### Before Starting Work

コードを変更する作業では、実装前に必ず次を行うこと。

1. `AGENTS.md`を読む
2. `docs/PROJECT_STATUS.md`を読む
3. `docs/TASKS.md`から現在の対象タスクを確認する
4. 必要に応じて`docs/DECISIONS.md`を確認する
5. `git status`と`git diff`を確認する
6. 関係する実装とテストを確認する
7. 管理文書と実際のコードに矛盾がないか確認する

管理文書を無条件に信用せず、コード、テスト結果、Gitの状態と照合すること。
矛盾がある場合は、実際の状態を調査してから管理文書を修正すること。

### During Work

- 原則として、指定されたタスクだけを実施する
- 別の問題を発見しても、現在のタスクと無関係なら勝手に修正しない
- 発見した別問題は`docs/TASKS.md`の候補として記録する
- 大きな方針変更は`docs/DECISIONS.md`に記録する
- タスクの完了条件を満たすまでは`done`にしない
- テスト未実施の場合は、未実施であることを明記する
- 推測した内容を確定事項として記載しない
- 生のログや長いコマンド出力を管理文書へ貼り付けない

### Before Finishing Work

コードを変更した作業の終了前に、必ず次を行うこと。

1. 変更内容を確認する
2. 実行可能なテスト、ビルド、型チェックを実行する
3. `docs/TASKS.md`の対象タスクを更新する
4. `docs/PROJECT_STATUS.md`を現在の状態に更新する
5. 重要な設計判断があれば`docs/DECISIONS.md`へ追加する
6. 未解決事項、テスト結果、次に行う作業を記録する
7. 最終回答で変更内容と確認結果を簡潔に報告する

ドキュメントは会話履歴の保存場所ではなく、
「現在の正しい状態」を次の担当者へ伝えるためのものとして更新すること。
