# Tasks

最終更新: 2026-08-03 (JST)

ステータスは `todo`、`in_progress`、`blocked`、`done` を使用する。

## 現在の対象

### T-017 Animagine Zero上でのCharacter・Style LoRA分離評価

- ステータス: `in_progress`
- 優先度: 高
- 背景:
  - v2は固定人物定義の自動挿入により、瞳色、顔年齢、後頭部のお団子が改善した
  - 脚の長さ・身長比率は元画像より高くなる場合がある
  - 画風はCharacter LoRAよりAnimagine XL 4.0の影響が強い
  - v3でもお団子が側頭部に出ており、髪型教師画像にも同じ誤配置が含まれている
- 実装内容:
  - 固定人物定義を含むcaptionでrank・step・学習対象層を比較し、体格再現を評価する
  - Character LoRAへ画風を混在させる案と、同一基盤モデル向けStyle LoRAを分離する案を比較する
  - 元画風、別衣装、別背景、正面・背面で検証し、人物同一性とプロンプト追従性を別々に採点する
- 完了条件:
  - 目色、顔、後頭部中央のお団子、身長比率を複数seedで評価する
  - 元画風を維持する方式を、他キャラクターへ転用可能な構成として決定する
  - 過学習した白背景・猫Tシャツが新しいシーンへ残らない
- ユーザーに用意を依頼する可能性があるもの:
  - Character LoRAと分離したStyle LoRAを作る場合、同じ画風で別人物・別背景を描いた画像
- 2026-07-29試験結果:
  - サブフォルダ分けされた99枚を検査し、完全重複なし、顔18・全身30・体格8・髪型13・要素分離30であることを確認
  - 高身長化した体格4枚、線・陰影が別系統の背景20枚、冗長な旧全身14枚を除き、61枚を採用
  - 人物固有の髪、瞳、お団子、白い猫型髪飾りをトリガーワードへ学習させ、衣装、背景、構図、表情を52種類のcaptionへ分離
  - Animagine XL 4.0 Opt、rank 16、768解像度、左右反転なしで800ステップ学習し、200・400・600・800を保存
  - 同一Seed比較では400ステップ版が最も小柄な体格を保ち、600・800は脚の長さと髪飾り位置のずれが増えた
  - 400ステップ版を`金髪ショートヘアの少女 v3（選別61枚）`としてローカル登録し、推奨強度0.8とした
  - 人物同一性はv2より改善したが、画風は依然Animagine XL 4.0の影響が強い。Style LoRA分離方針は維持する
  - Animagine XL 4.0 Zeroをローカル・Vast共通の既定基盤として実装した
  - 基盤profileを `sdxl-animagine-zero` としてLoRAメタデータへ保存し、旧Opt/JANKU用LoRAを非互換として隔離した
  - Character LoRAとStyle LoRAを別セレクタ・別強度で同時ロードするUI、API、推論処理、メタデータ保存を実装した
  - Style LoRAはrank 32、学習率 `5e-5`、左右反転あり、50枚以上推奨へ分離した
  - Character LoRAに固定除外条件を追加し、`side bun, bun beside ear`などをnegative promptへ自動挿入できるようにした
  - 自動テスト46件、Python・JavaScript・Bash構文、Compose設定を確認した
  - Zero単体で海辺・金髪・ピンク色の瞳・白いサマードレスを768×1152で実生成し、`sdxl-animagine-zero` のメタデータを確認した
  - ローカルUIでCharacter・Styleの別セレクタ、カテゴリ別推奨枚数、Style選択時の人物固定項目非表示を確認した
- 2026-07-31 Zero再学習データ準備・初回学習:
  - `input/学習用テストデータ/金髪ショートヘアの少女_Zero再学習/` に採用36枚を整理した
  - 内訳はターンアラウンド12、表情差分12、白背景の全身基準6、衣装・背景・ポーズ差分6
  - 採用36枚に完全重複はなく、最小短辺は640px
  - 表情カタログ、側頭部にお団子が出た正面全身、重複画像などは `00_カタログ`、`99_要修正`、`非採用` へ分離した
  - 正面ではお団子を描かず、横・背面・振り返りで後頭部中央の位置を教える。正面から無理に見せる画像は採用しない
  - 瞳色は旧 `#ff66cc` 固定ではなく、採用画像を基準とするRose Crimsonを最終仕様とした
  - 背景・衣装・表情・ポーズを分散し、白背景や同一衣装だけへの過学習を避ける構成にした
  - 採用36枚を目視監査し、人物固定要素のうち画面に見えるものと、視点、構図、表情、ポーズ、衣装、背景を分離した同名 `.txt` captionを36件作成した
  - 屋上画像の猫柄衣装・バッグ、室内画像の背景の猫を可変要素としてcaptionへ明記し、人物固有の猫型髪飾りと分離した
  - 正面や背面など、視点のため見えないお団子、瞳、髪飾りはcaptionへ無理に記載していない
  - Zeroの両CLIP tokenizerで、trigger追加後の最大caption長は71トークン、77超過0
  - 正本を変更せず、`input/学習用テストデータ/金髪ショートヘアの少女_Zero再学習_学習用フラット/` に画像36・caption 36の学習用ステージングを作成した
  - dry-runで画像36、ユニークcaption 35、左右反転なし、縦長を含む6種類のbucket、rank 16、解像度768、学習率 `1e-4`、720ステップを確認した
  - Animagine XL 4.0 Zeroで720ステップ学習し、200・400・600・720ステップの比較重みを保存した
  - ローカル登録idは `a45c8b0d48f44490bd656227730a3f09`。720ステップ版を暫定重みとし、LoRA smoke loadに成功した
  - 同一条件の正面、正面のお団子指定なし、斜め、背面、全身を実GPU比較した
  - 正面でお団子指定を外すと全checkpointで側頭部のお団子が消え、720がRose Crimsonの瞳と猫型髪飾りを最もよく再現した
  - 背面中央のお団子は600が最も近い一方、720では消失した。全身は全checkpointで高身長・長脚傾向が残った
  - 1つのcheckpointで全完了条件を満たさないため、720は `provisional_not_accepted` とし、T-017は `in_progress` を維持する
  - 比較画像と条件は `output/t017-zero-lora/` に保存した
- 残作業:
  - 人物固定定義を視点依存にし、正面では後頭部のお団子を自動挿入しない方法を検討する
  - 体格教師画像とお団子教師画像の比率、caption、repeat数を見直して再学習する
  - 複数seedの正面・左右斜め・背面・全身・別衣装・別背景で、単一checkpointが完了条件を満たすまで比較する
  - Style LoRA用に、同じ画風で別人物・別背景・複数構図を含む50枚以上の基準画像を確定する
  - Style LoRAを別人物・別背景・複数構図で学習し、他キャラクターへの転用性を評価する
  - Zero単体、Zero＋Character、Zero＋Character＋Styleを同一Seedで実GPU比較する

### T-018 長い人物固定定義下でのシーン固有タグ保持

- ステータス: `done`
- 優先度: 高
- 背景:
  - v3の実Web API評価で、入力した`seaside train station, ocean, sunset`が最終promptでは`outdoors, blue sky`へ縮約された
  - 人物固定定義、衣装、ポーズ、品質タグを優先した結果、海、駅、夕方というシーン固有情報がCLIP上限で脱落している
- 実装内容:
  - 人物固有属性と同様に、ユーザーが明示した場所、時間帯、天候を優先タグとして保持する
  - 一般タグへ置換する場合も、固有タグを削除せず短い表現へ圧縮する
  - 日本語最適化あり・なし、LoRAあり・なしで優先順をテストする
- 完了条件:
  - `seaside train station, ocean, sunset`が人物固定定義と同時に最終promptへ残る
  - 海、教室、夜の街など複数シーンで人物属性を落とさず背景指示を反映する
- 2026-07-31 実装・確認結果:
  - 人物・動作・シーンの明示タグを抽出し、Qwen出力が一般語へ縮約しても最終promptへ短い形で再挿入するよう修正した
  - 人物数は `1girl, solo` を前方へ固定し、シーン込み全身指定では縦長キャンバスと単一構図を優先して、複数人物・分割構図を抑制した
  - 画風語と品質語より、人物固有属性、今回の動作、場所、時間帯を優先してCLIP上限内へ圧縮するようにした
  - `seaside train station, ocean, sunset`、教室、夜の街を含む回帰テストを追加し、人物属性とシーン固有タグが共存することを確認した
  - ユーザー指定のVサイン、ガラスの林檎のピアス、田んぼ道、放課後、夕方を使った実Web生成で、単一人物・背景・構図が概ね改善したことをユーザー確認済み

### T-019 逐次複数生成と画風設定の保存・履歴復元

- ステータス: `done`
- 優先度: 高
- 背景:
  - 生成UIとSSEは1枚前提で、同じ条件の候補を複数まとめて比較できなかった
  - 画風の強さは再読み込みでテンプレート既定値へ戻り、生成履歴からも復元されなかった
- 実装内容:
  - 新規生成と一貫再生成へ1〜8枚の生成枚数入力を追加し、GPU上では必ず1枚ずつ直列生成する
  - 1枚目は入力Seedを維持し、2枚目以降は重複しないランダムSeedだけを変える。prompt、画風、構図、CFGなどは同一に保つ
  - 完成画像をSSEで1枚ずつ返し、結果画面にSeed付きの選択可能な一覧を表示する
  - 選択中の画像を保存、R2保存、一貫再生成の対象として扱う
  - 4つの画風強度をブラウザへ保存し、再読み込み後に復元する
  - 生成履歴へ画風強度を保存し、「プロンプトを履歴から参照」で入力promptと同時に復元する
- 完了条件:
  - 指定枚数を並行実行せず、異なるSeedで順番に生成できる
  - 複数結果から対象画像を明確に選択できる
  - 画風強度が再読み込みと履歴参照の両方で復元される
- 2026-07-31 確認結果:
  - 自動テスト57件、Python構文、Docker buildが成功した
  - 実Web UIで2枚をSeed `32` と別のランダムSeedで順番に生成し、2列結果、選択状態、保存・再生成対象の切替を確認した
  - 画風強度を73へ変更して再読み込み後も73を維持し、20へ変更後に最新履歴を参照すると73へ戻ることを確認した
  - 履歴適用後の再読み込みでも73を維持し、ブラウザコンソールエラー0件を確認した

### T-020 ユーザー別LoRAのR2永続化

- ステータス: `in_progress`
- 優先度: 高
- 背景:
  - Vast.aiの `/models` はインスタンス交換で失われる可能性があり、学習済みLoRAをサービスの正本にできない
  - Cloudflare Worker経由では利用者IDが `X-App-User-Id` としてVastへ渡され、既存のローカルLoRAもユーザー別ディレクトリへ分離される
  - 今後はCharacterだけでなくStyleなど複数カテゴリを利用者ごとに学習・再利用する
- 実装内容:
  - R2の `loras/v1/owners/<owner-key>/models/<model-id>` をユーザー別正本にし、生の利用者IDをオブジェクトキーへ含めない
  - 学習完了後に推論重み、学習設定、メタデータを自動アップロードする
  - LoRA一覧と生成時に要求された利用者だけを遅延同期し、サイズとSHA-256を検証して `/models/loras` へ復元する
  - Character、Style、Pose、Backgroundを共通形式で保存し、基盤モデルprofileの互換性判定は既存処理を維持する
  - 教師画像とcaptionのバックアップは環境変数による明示的なopt-inにする
  - 推論に不要な中間checkpointは既定で除外し、再公開で参照されなくなった同一モデルのR2成果物を整理する
  - 既存ローカルLoRAを同一ownerまたは指定したremote owner keyへ移行するCLIを追加する
- 完了条件:
  - 別ユーザーのLoRAが一覧・同期・生成対象へ混ざらない
  - CharacterとStyleの両方を新しいローカルストアへ復元できる
  - 破損した重みをチェックサム検証で拒否する
  - 学習成功とR2保存失敗を分離し、保存失敗時もローカルの完成重みを使用できる
  - 自動テスト、Python構文、Docker build、秘密情報を含まない設定例を確認する
  - GitHubと新しいSHA固定Dockerイメージへ反映し、Vast.aiでユーザー別R2同期をend-to-end確認する
- 2026-08-03 ローカル実装・確認:
  - ユーザー別R2同期、学習後アップロード、遅延復元、チェックサム検証、任意の教師画像保存、移行CLIを実装した
  - R2を模擬したCharacter・Style復元、ユーザー分離、ローカルからcloud ownerへの移行、教師画像opt-in、中間checkpoint除外・整理、破損拒否を含む自動テスト64件が成功した
  - 変更Pythonファイルの構文、Compose設定、Docker buildが成功した
  - ビルド済みイメージにR2同期コードが入り、`docs/KEYS.md` と `output/` が含まれないことを確認した
  - 現在のZero Character LoRAをR2の `local` ownerへ推論重みと学習設定の2成果物、93,065,511 bytesとして保存した
  - 中間checkpoint 4件を未参照成果物としてR2から整理し、cleanup error 0件を確認した
  - 空の一時ストアへR2から1モデルを復元し、サイズ・SHA-256検証とメタデータ登録に成功した
  - checkpoint除外修正コミット `9b1f096ce4ca5ad4e54871bba7b095f603b41479` をGitHubへpushした
  - 同コミットのDocker Hubイメージを公開し、リモートdigest `sha256:196e8377cf1a5ad0969ec042b74a086a03087ad9759e241203a1980363915f02` と `linux/amd64` manifestを確認した
  - Cloudflare Access利用者ownerへZero Character LoRAを移行し、Vast.aiの一覧で1件を互換・readyとして復元した
  - LoRA選択生成はAnimagine本体の起動時取得失敗により未完了。R2同期やLoRA互換性ではなく、ベースモデルが0 bytesのまま存在しないことが直接の阻害要因
- 残作業:
  - Hugging Face取得再試行修正版をVast.aiへ反映し、復元済みZero Character LoRAで生成を確認する

### T-015 OpenPose ControlNetによる厳密なポーズ制御

- ステータス: `todo`
- 優先度: 高
- 背景: Character LoRAとIP-Adapterは人物同一性に有効だが、顔元のVサインなど手指を含むポーズをテキストだけで毎回保証できない
- 実装内容:
  - SDXL互換OpenPose ControlNetを生成パイプラインへ追加する
  - ポーズ参照画像またはアプリ内ポーズテンプレートを入力できるようにする
  - 骨格・手の制御強度をLoRA強度、IP-Adapter強度から独立させる
  - ギャラリーからの一貫再生成へポーズ参照を引き継ぐ
  - ポーズ画像がない場合は現在のテキスト制御へフォールバックする
- 完了条件:
  - 指定キャラクターを単一人物として維持したまま、海、教室など複数背景で顔元のVサインを高確率に再現できる
  - ポーズ制御を無効にした通常生成と、LoRA・IP-Adapter併用生成を壊さない
  - 実GPU評価、自動テスト、Dockerビルドを通す
- ユーザーに用意を依頼するもの:
  - 使用したい代表ポーズの参照画像、または採用したいポーズテンプレートの方針

### T-002 `.env.local.example` の追加

- ステータス: `todo`
- 優先度: 高
- 背景: `README.md` と `scripts/local.ps1` が参照しているがファイルが存在しない
- 完了条件: 秘密情報を含まない設定例を追加し、記載値とDocker Composeの参照を整合させる

### T-003 最新Vast.aiイメージの起動確認

- ステータス: `in_progress`
- 優先度: 高
- 完了条件: 最新UI、モデル準備、Flask起動、参照生成、通常生成をVast.aiで確認する
- 2026-08-01 公開準備:
  - ローカル評価画像の `output/` を `.dockerignore` へ追加し、公開イメージへ混入しないよう修正した
  - コミット `c8233dcaba06d4cf180bd31e9bc8006b191faf34` からVast.ai用イメージをビルドした
  - `nukota0615/hidream-o1-image:c8233dcaba06d4cf180bd31e9bc8006b191faf34` をDocker Hubへ公開した
  - 公開digestは `sha256:f9f6df2467bb58725f677005c19a957a61aac98ac79cc2be184a64a1ab3ab44b`、platformは `linux/amd64`
  - GHCRへの同タグ公開はローカル認証切れで未実施。Vast.aiでは公開済みDocker Hubタグを使用できる
  - ユーザー方針により、現在のローカルマシンと同等のVast.ai構成を後日選定する。インスタンス作成と課金は未実施
- 2026-08-03: 構成選定を再開した。ローカルはRTX 5090・約32GB VRAMで、Vast.aiのログイン・残高確認後に候補を比較する
- 2026-08-03: ユーザーがVast.aiテンプレートとCloudflare環境変数を設定した。次は現在使用中のモデルだけを準備する新しいSHA固定イメージへテンプレートを更新し、ユーザー側で起動確認する
- 2026-08-03 現行モデル限定イメージ公開:
  - `nukota0615/hidream-o1-image:4b7d43c33ce7452cd4a7cb9e6da90011d9d6840f` をDocker Hubへ公開した
  - リモートdigest `sha256:14ef1788ac1b8dcc632906e065cd60274248e0114a8a918be8319865ca053df5`、実行platform `linux/amd64` を確認した
  - 最初のVastホストではregistry layer展開に失敗したが、別ホストへ変更後は起動と通常イラスト生成に成功した
  - 残作業は参照画像あり生成と、T-020のLoRA復元を含む最新イメージの確認
  - T-020イメージではLoRA一覧への復元に成功したが、Animagine本体が0 bytesのまま生成待機した。Qwen取得ログにはHugging Face Xet/CASの応答デコードエラーが記録されていた
  - Xet/CASを既定で無効にし、通常HTTP取得を最大5回再試行する起動処理と、最終失敗を生成要求へ即時通知するマーカーをローカル実装した
  - 自動テスト66件、Python・Bash構文、Compose設定、Vast用Docker buildに成功
  - 修正コミット `45e2bca2bc9a27fdb475c75fac2700ae7d50c4a7` と同SHAのDocker Hubイメージを公開し、digest `sha256:98f4ff43961d91d8b5e4c034e259df10354e7c52246e64b55cdffbf052133c30`、`linux/amd64` manifestを確認した
  - 残作業は修正版イメージのVast.ai再デプロイ、モデル取得完了、LoRA選択生成、参照画像あり生成の確認

### T-004 Worker・Vast.ai・R2・D1のend-to-end確認

- ステータス: `todo`
- 優先度: 高
- 依存: T-003
- 完了条件: Worker経由の生成、履歴、画像保存、フォルダ、お気に入り、ユーザー分離を確認する
- 2026-08-03 Cloudflare事前確認:
  - 新しいアカウントAPIトークンがactiveで、Worker、R2、D1の既存リソースを読み取れることを確認した
  - 新しいR2 S3認証で画像バケットとモデルキャッシュの読み取りに成功した
  - 既存WorkerのR2・D1・Access・Backend bindingと、D1のgallery用テーブルを確認した
  - 現在の `BACKEND_SHARED_SECRET` は `plain_text` bindingのため、Vast URL確定後の再デプロイでWorker Secretへ置き換える
  - 検証用APIトークンとR2 S3キーは本番稼働前にローテーションする

### T-005 Worker依存の脆弱性調査

- ステータス: `todo`
- 優先度: 中
- 背景: `npm install` 時にhigh severity警告が報告されている
- 完了条件: `npm audit` で影響範囲を確認し、互換性を保てる更新方針を決める

### T-008 コンテナ再作成時の依存セットアップ短縮

- ステータス: `in_progress`
- 優先度: 中
- 完了条件: モデルをDockerイメージへ含めず、依存レイヤーまたはローカル専用永続化で再作成時間を短縮し、Vast.aiのpull時間とのバランスを確認する
- 2026-07-29:
  - ローカルComposeへ外部volume `janku-python-local` を追加し、`/venv/main`のPython/CUDA依存を再作成後も保持するようにした
  - 任意のHiDream取得失敗が標準アプリを再起動ループへ入れないよう、警告継続へ変更した
  - コンテナを2回再作成し、2回目以降は `Python and CUDA dependencies are already installed.` となり再インストールが省略されることを確認した
- 2026-08-03:
  - Vast.aiの標準prefetchをAnimagine XL 4.0 Zero、SDXL IP-Adapter Plus、Qwen3.5-9Bだけに整理した
  - 未使用のJANKU、Waifu-Inpaint、anime segmentation、FLUX、Qwen-Image-Edit、HiDream用変数を標準環境変数例から除外した
  - 従来はfresh diskで実行されていたHiDreamソース・追加依存のセットアップを `HIDREAM_RUNTIME_SETUP_ON_START=1` の明示的なopt-inへ変更し、既定を無効にした
  - Worker経由のR2保存ではVast側のCloudflare APIトークン・R2 S3認証が不要であることをデプロイ手順へ明記した
  - 自動テスト57件、主要Python 5ファイルとBashの構文、Compose設定、Docker buildが成功した
  - ローカル秘密情報ファイルと評価用 `output/` がビルド済みイメージへ含まれないことを確認した
  - Vast実機でHugging Face Xet/CASの応答デコードエラーを確認したため、`HF_HUB_DISABLE_XET=1`、120秒のdownload timeout、最大5回の段階的再試行を標準化した
  - Animagine取得が全試行で失敗した場合は失敗マーカーを保存し、生成要求が2時間待機せず原因を返すようにした
  - 再試行修正版をコミット `45e2bca2bc9a27fdb475c75fac2700ae7d50c4a7` のDocker Hubイメージとして公開した
- 残作業:
  - 再試行修正版のSHA固定イメージをVast.aiで起動し、Animagine・IP-Adapter・Qwenの取得完了とfresh diskの所要時間を実測する
  - Vast.aiの永続ディスクで同等の依存キャッシュを採用するか、イメージサイズと比較する

### T-016 実用Character LoRAの品質評価

- ステータス: `done`
- 優先度: 高
- 完了日: 2026-07-28
- 内容:
  - 同一人物30枚を形式、重複、人物一貫性、ポーズ多様性の観点で検査
  - 画像別キャプションを作成し、衣装・背景・ポーズと人物固有属性を分離
  - 1024×1536の全身を切らない640×960バケットを実装
  - 非対称な横お団子と髪留めを守るためランダム左右反転を無効化
  - 200・400・600ステップを比較し、人物定義と併用した400ステップ版を採用
  - `猫Tシャツの少女 v2`、トリガー `nkt_chr001` としてローカルアプリへ登録
  - LoRAなし、LoRAのみ、LoRA＋参照25%を同一Seedで実GPU比較
  - 白背景由来の白枠をLoRAメタデータの学習混入タグとnegative制約で抑制
  - 海シーンの海面・水平線を優先する制約を新規一枚絵にも適用
- 推奨値:
  - LoRA強度: 0.8
  - IP-Adapter参照: 人物優先25%、ポーズ優先20%
  - 自動学習ステップ: 画像数×20、最小200、最大800
- 確認結果:
  - 2026-07-29の再評価で、暫定仕様をピンク系の瞳と後頭部中央のお団子へ訂正した。瞳色の最終仕様は2026-07-31に採用画像基準のRose Crimsonへ更新
  - LoRA固定人物定義の自動挿入により、目色、顔年齢、後頭部のお団子を改善
  - 黒い猫Tシャツから赤い制服・白いサマードレスへ変更
  - 白背景から海辺・海の見える屋外へ変更
  - 自動マスク、クロマ境界、白枠なし
- 制約:
  - 全30枚が同じ衣装・白背景のため、トリガーワード単独より保存済み人物定義との併用が安定する
  - 体格比率と学習画像の画風はv2で完全再現できず、T-017で継続する
  - 手振りなど厳密な手のポーズはT-015のOpenPoseで対応する

## 方針変更で置き換えたタスク

### T-009 立ち絵レイヤーの保存・再利用強化

- ステータス: `done`（D-013・T-014により置き換え）
- 理由: 自動人物抽出と背景マスクを正本にせず、LoRA・参照画像・人物定義を再利用資産として全画面再生成する方針へ変更した
- 備考: 完全な透明立ち絵が必要な個別用途は、将来のネイティブ透過生成または手動処理として別途扱う

## 完了済み

### T-014 マスクレスなキャラクター一貫再生成

- ステータス: `done`
- 完了日: 2026-07-28
- 内容:
  - 人物入力とシーン入力を別々に最適化し、一回の全画面生成へ統合
  - 自動輪郭マスク、クロマキー、背景inpaintを標準UIから除外
  - SDXL IP-Adapter Plus一枚参照とCharacter LoRAの同時利用
  - 人物中心参照クロップ、既定参照強度25%、元シーンの負条件化
  - 参照あり生成後のIP-Adapter解除
  - 単一人物、変更ポーズ、人物属性、シーンのタグ優先制御
  - ギャラリーと結果画面から人物定義、LoRA、参照画像、生成設定を一貫再生成へ引き継ぐ
  - Waifu-Inpaint-XLを手動マスク局所修正だけに限定
- 確認:
  - 自動テスト34件成功
  - Python、JavaScript、Bash構文確認とDockerビルド成功
  - 実GPUでピンク色のショートヘアの女子学生を、夕方の教室から海・砂浜へマスクなしで再生成
  - 単一人物、ピンク髪、制服、海背景を確認
  - 標準フローにマスク境界が存在しないことを確認
- 制約:
  - Character LoRAの実GPU評価はT-016で完了
  - テキストとIP-Adapterだけでは顔元のVサインが確率的であり、厳密なポーズ制御はT-015へ分離

### T-013 キャラクター資産生成ワークフローの再設計

- ステータス: `done`
- 完了日: 2026-07-28
- 内容: 人物・シーン分離入力、LoRA学習UI・ジョブ、ギャラリー起点の追加学習、ユーザー別資産管理を実装
- 備考: 自動背景inpaint部分はD-013・T-014で全画面再生成へ変更

### T-012 人物内部の透過とクロマ境界残りの回帰修正

- ステータス: `done`（旧方式）
- 完了日: 2026-07-28
- 備考: マスク調整では髪の隙間、半透明素材、極細の毛先を同時に解決できないため、標準フローはT-014へ移行

### T-011 クロマ境界の色縁・マスク残り修正

- ステータス: `done`（旧方式）
- 完了日: 2026-07-27
- 備考: 既存資産との互換用コードのみ残す

### T-010 フリー入力と人物境界マスクの回帰修正

- ステータス: `done`
- 完了日: 2026-07-27
- 備考: フリー入力の優先とQwen最適化は継続。自動人物境界マスクは標準から廃止

### T-007 背景変更時のキャラクター保持強化

- ステータス: `done`（旧方式）
- 完了日: 2026-07-27
- 備考: 自動人物抽出方式はT-014へ置き換え

### T-001 ローカル開発環境の再現確認

- ステータス: `done`
- 完了日: 2026-07-27
- 内容: Dockerイメージのビルド、コンテナ起動、UI、ヘルスチェック、GPU生成を確認

### T-000 タスク・進捗管理文書の導入

- ステータス: `done`
- 完了日: 2026-07-22

## 将来タスク

- Google IdPと一般登録ポリシー
- ユーザー別生成回数・GPU利用量制限
- ジョブキュー、タイムアウト、リクエストサイズ制限
- 監査ログと異常検知
- 本番向けWSGIサーバー
- ギャラリー画像削除
- R2/D1バックアップと削除方針
- ローカルとVast.aiの生成モデル統一
